{
  "name": "googleapis-common-fixture",
  "version": "1.0.0",
  "description": "An app we're using to test the library. ",
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "start": "node build/src/index.js"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "googleapis-common": "file:./googleapis-common.tgz"
  },
  "devDependencies": {
    "@types/node": "^10.3.0",
    "typescript": "^3.0.0",
    "gts": "^1.0.0",
    "null-loader": "^0.1.1",
    "ts-loader": "^5.0.0",
    "webpack": "^4.20.2",
    "webpack-cli": "^3.1.1"
  }
}
